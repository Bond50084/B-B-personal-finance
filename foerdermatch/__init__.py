"""FoerderMatch — RAG matching of clients to German subsidy programmes.

Importable as a package (e.g. from a Flask app):

    from foerdermatch.config import settings
    from foerdermatch.matching_engine import MatchingEngine

and still runnable standalone from inside the folder:

    python main.py demo
"""
