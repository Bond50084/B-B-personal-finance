# FoerderMatch MVP — Python dependencies
# Pinned to the versions verified during development (Python 3.10+).

# Vector store
chromadb==1.5.9

# LLM / embedding abstraction layer
langchain-core==1.4.6

# Optional providers (only needed for the matching provider you choose).
# Mock mode (the default) requires none of the SDK calls but the packages
# must be importable; comment out the ones you do not use.
langchain-openai==1.3.0
langchain-ollama==1.1.0
langchain-anthropic==1.4.6
langchain-google-genai==4.2.5

# Live scraping of foerderdatenbank.de (only needed for `ingest --source live`)
requests==2.33.1
beautifulsoup4==4.14.3

# Configuration via .env
python-dotenv==1.2.2
